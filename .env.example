# .env
PORT=3000
# Bind address. Unset listens on all interfaces.
# HOST=127.0.0.1
DB_PATH=./data/demo.db
DB_BUSY_TIMEOUT_MS=5000
LOG_DIR=./logs
LOG_LEVEL=info
# * allows any origin. Separate several origins with commas.
CORS_ORIGIN=*
