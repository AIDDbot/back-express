colors: false
no_tty: true
pre-commit:
  parallel: true
  commands:
    format:
      glob: "*.{ts,tsx,json,md}"
      run: nub run format {staged_files}
      stage_fixed: true
    lint:
      run: nub run lint
      run: nub run check
      stage_fixed: true

# pre-push:
#   commands:
#     typecheck:
#       run: oxlint --type-aware --type-check .
#     test:
#       run: nub run test
