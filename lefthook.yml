assert_lefthook_installed: true
colors: false
no_tty: true
pre-commit:
  commands:
    debug:
      run: 'echo "staged: {staged_files}"'
    # format:
    #   run: nub run format
    #   stage_fixed: true
    # lint:
    #   run: nub run lint
    #   stage_fixed: true

# pre-push:
#   commands:
#     typecheck:
#       run: oxlint --type-aware --type-check .
#     test:
#       run: nub run test
