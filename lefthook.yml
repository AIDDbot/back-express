colors: false
no_tty: true
pre-commit:
  parallel: true
  commands:
    # lint:
    #   run: nub run lint
    #   stage_fixed: true
    format:
      glob: "*.{ts,tsx,json,md}"
      run: nub run format {staged_files}
      stage_fixed: true

# pre-push:
#   commands:
#     typecheck:
#       run: oxlint --type-aware --type-check .
#     test:
#       run: nub run test
