colors: false
no_tty: true
pre-commit:
  commands:
    lint:
      run: nub run lint
      run: nub run check
      stage_fixed: true
    format:
      run: nub run format
      stage_fixed: true

# pre-push:
#   commands:
#     typecheck:
#       run: oxlint --type-aware --type-check .
#     test:
#       run: nub run test
